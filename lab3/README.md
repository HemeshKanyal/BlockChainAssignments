# Lab 3: Hardhat Project

This folder contains the work for Lab 3.

## Structure
- `hardhat-project/`: Main Hardhat project directory

## Setup
Navigate to `hardhat-project` and install dependencies:
```bash
cd hardhat-project
npm install
```
Configure `.env` with your private key and RPC URL.
